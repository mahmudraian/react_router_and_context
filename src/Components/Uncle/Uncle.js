import React from 'react';

const Uncle = ({house}) => {
    return (
        <div>
            <h4>Uncle</h4>
            <p><small> house={house}</small></p>
        </div>
    );
};

export default Uncle;